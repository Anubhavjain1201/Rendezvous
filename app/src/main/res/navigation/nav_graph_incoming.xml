<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph_incoming"
    app:startDestination="@id/incomingMeetingFragment">

    <fragment
        android:id="@+id/incomingMeetingFragment"
        android:name="com.example.rendezvous.fragments.IncomingMeetingFragment"
        android:label="IncomingMeetingFragment" >
        <argument
            android:name="displayName"
            app:argType="string"
            app:nullable="true" />
        <argument
            android:name="meetingType"
            app:argType="string"
            app:nullable="true" />
    </fragment>
</navigation>